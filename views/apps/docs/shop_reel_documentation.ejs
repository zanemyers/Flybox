<h1>🎣 ShopReel Documentation</h1>

<p>ShopReel scrapes business data from <strong>Google Maps</strong> using <strong>SerpAPI</strong> and individual shop websites, compiling the results into a structured Excel file.</p>

<hr>

<h3>Get Your SerpAPI Key</h3>
<ul>
    <li>Go to <a href="https://serpapi.com/" target="_blank">SerpAPI</a> and create a free account.</li>
    <li>After signing up, an API key will be automatically generated for your account.</li>
    <li>Locate the section called <code>Your Private API Key</code> and copy the key for use in ShopReel.</li>
</ul>
<div class="d-flex justify-content-center">
    <img src="/images/serpApi.png" alt="Your Private API Key" class="img-fluid w-75">
</div>


<hr>

<h3>Using the Search Form</h3>

<p>The ShopReel interface allows you to configure searches via a form with manual inputs or by importing an Excel file acting as a cache.</p>

<h5>Manual Input</h5>
<p>Use this tab to enter search parameters directly for a custom ShopReel run. Default values are provided for convenience, but you can adjust them as needed.</p>

<ul>
    <li>
        <strong>SerpAPI Key:</strong> Enter your private API key in the field provided.<br>
        <em>Note:</em> This key is required for ShopReel to access Google Maps data via SerpAPI.
    </li>
    <li>
        <strong>Search Term:</strong> Specify the type of business or shop you want to search for (e.g., "Fly Fishing Shops", "Tackle Stores").<br>
        <em>Default:</em> <code>Fly Fishing Shops</code>
    </li>
    <li>
        <strong>Location:</strong> Enter latitude and longitude in decimal degrees. You can also click the map button to pick a location visually.<br>
        <em>Default:</em> Yellowstone National Park: <code>Latitude: 44.427963</code>, <code>Longitude: -110.588455</code>.<br>
    </li>
    <li>
        <strong>Max Results:</strong> Set the maximum number of results to retrieve. ShopReel fetches results in batches of 20, so entering <code>101</code> could yield up to <code>120</code> results.<br>
        <em>Default:</em> <code>100</code>.<br>
    </li>
</ul>

<div class="d-flex justify-content-center pb-3">
    <img src="/images/shopReel_mForm.png" alt="ShopReel Manual Input Form" class="img-fluid w-75">
</div>


<p>Once all fields are set, click the <strong>Search</strong> button. Progress indicators will be displayed on the page.</p>


<h5>File Import</h5>
<ul>
    <li>Import a previously exported <strong>simple_shop_details.xlsx</strong> to reuse shop data.</li>
    <li>This avoids redundant requests to SerpAPI and speeds up processing.</li>
</ul>

<div class="d-flex justify-content-center pb-3">
    <img src="/images/shopReel_fForm.png" alt="ShopReel File Import Form" class="img-fluid w-75">
</div>


<p>After selecting the file, click the <strong>Search</strong> button. Progress indicators will be displayed on the page.</p>

<h5>Output Files</h5>
<p>After running a search, ShopReel generates Excel files containing the shop data. The behavior depends on how you run the search:</p>

<ul>
    <li><strong>Manual Input:</strong>
        <ul>
            <li><strong>simple_shop_details.xlsx:</strong> Generated immediately after the SerpAPI search completes. Contains basic shop information and can be reused in subsequent runs to avoid additional API requests for the same dataset.</li>
            <li><strong>shop_details.xlsx:</strong> Includes detailed information collected from each shop’s website (emails, online store availability, social media profiles, etc.).</li>
        </ul>
    </li>
    <li><strong>File Import:</strong>
        <ul>
            <li>Only the final <strong>shop_details.xlsx</strong> file will be downloaded, since the <strong>simple_shop_details.xlsx</strong> file is already provided for reuse.</li>
        </ul>
    </li>
</ul>

<hr>

<h3>⚠️ Disclaimers</h3>
<ul>
    <li><strong>Email scraping:</strong> Not always accurate; some addresses may be outdated or missing.</li>
    <li><strong>Blocked pages:</strong> Some business websites may block Playwright or fail to load. Fallback data will be provided.</li>
    <li>Use responsibly and comply with websites’ terms of service.</li>
</ul>

<hr>

<h3>✏️ Additional Notes</h3>
<ul>
    <li>The app supports both cached Excel files and live scraping via SerpAPI to maximize efficiency.</li>
    <li>Future versions may support alternate APIs and extended filtering options.</li>
</ul>
